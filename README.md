# 🍽️ نظام إدارة مخزون المطعم
# Restaurant Inventory Management System

نظام متكامل لإدارة مخزون المطعم مبني باستخدام **Streamlit** و **Pandas**.

---

## 📋 الميزات الرئيسية

- ✅ إدخال المبيعات اليومية
- ✅ حساب المكونات المطلوبة تلقائياً
- ✅ تحديث المخزون بشكل آلي
- ✅ تحذيرات عند نقص المخزون
- ✅ لوحة معلومات تفاعلية
- ✅ إضافة مخزون جديد
- ✅ واجهة عربية كاملة

---

## 🚀 طريقة التشغيل

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. تشغيل التطبيق

```bash
streamlit run app.py
```

سيتم فتح المتصفح تلقائياً على العنوان: `http://localhost:8501`

---

## 📁 هيكل الملفات

```
autometino/
├── app.py              # التطبيق الرئيسي
├── inventory.csv       # بيانات المخزون
├── recipes.csv         # بيانات الوصفات
├── requirements.txt    # المتطلبات
└── README.md          # هذا الملف
```

---

## 📊 هيكل البيانات

### inventory.csv (المخزون)
| العمود | الوصف |
|--------|-------|
| Ingredient | اسم المكون |
| Current_Stock | الكمية الحالية |
| Unit | وحدة القياس |

### recipes.csv (الوصفات)
| العمود | الوصف |
|--------|-------|
| Dish_Name | اسم الطبق |
| Ingredient | المكون المطلوب |
| Quantity_Needed | الكمية لكل طبق |

---

## 📦 تحويل التطبيق إلى ملف EXE

### الخطوة 1: تثبيت PyInstaller

```bash
pip install pyinstaller
```

### الخطوة 2: إنشاء ملف التشغيل

أنشئ ملف `run_app.py` بالمحتوى التالي:

```python
import subprocess
import sys
import os

# الحصول على مسار التطبيق
app_path = os.path.join(os.path.dirname(__file__), 'app.py')

# تشغيل Streamlit
subprocess.run([sys.executable, '-m', 'streamlit', 'run', app_path])
```

### الخطوة 3: إنشاء ملف EXE

```bash
pyinstaller --onefile --noconsole run_app.py
```

### ⚠️ ملاحظات هامة

**طريقة بديلة (مستحسنة):**

بسبب طبيعة Streamlit، الطريقة الأفضل هي إنشاء ملف `.bat` للتشغيل:

1. أنشئ ملف `تشغيل_النظام.bat`:

```batch
@echo off
cd /d "%~dp0"
streamlit run app.py
pause
```

2. يمكنك تحويل ملف BAT إلى EXE باستخدام أدوات مثل:
   - [Bat To Exe Converter](https://bat-to-exe-converter-x64.en.softonic.com/)
   - [Advanced BAT to EXE Converter](https://www.battoexeconverter.com/)

### الطريقة المتقدمة باستخدام Nuitka

```bash
pip install nuitka
nuitka --standalone --onefile run_app.py
```

---

## 🔧 التخصيص

### إضافة أطباق جديدة
أضف صفوف جديدة في ملف `recipes.csv`:
```csv
اسم_الطبق_الجديد,المكون1,0.2
اسم_الطبق_الجديد,المكون2,0.1
```

### تعديل حد المخزون المنخفض
استخدم الشريط الجانبي في التطبيق لتعديل حد التنبيه.

---

## 📞 الدعم

للمساعدة أو الاستفسارات، يرجى التواصل مع فريق التطوير.

---

**تم التطوير بـ ❤️ باستخدام Python & Streamlit**
